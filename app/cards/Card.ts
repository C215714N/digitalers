export default class Card{
    symbol:string;
    value:number;
    icon:string;

    constructor(symbol:string, value:number, icon:string){
        this.symbol = symbol
        this.value = value
        this.icon = icon
    }
}